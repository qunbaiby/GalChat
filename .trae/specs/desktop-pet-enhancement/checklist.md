- [x] 桌宠专属提示词成功包含了当前的好感度阶段、性格特质、世界观和核心记忆。
- [x] 桌宠的上下文能够随着主游戏的进度更新，但仍拥有独立于主场景的聊天历史。
- [x] 桌宠界面中的固定 `ChatLabel` 被移除或隐藏，对话以动态生成的独立气泡显示。
- [x] 气泡中的文字具有打字机逐字显示效果（基于文本长度计算速度）。
- [x] 气泡中的动作描写（如 `（微笑）` 或 `(...)`）能够被正则表达式匹配并渲染为绿色。
- [x] 带有 `[SPLIT]` 的多段回复能够被正确分割为多个气泡，并分段展示。
- [x] 新气泡生成时，旧气泡会平滑上移（最多保留3个），并在设定时间后渐隐销毁。
- [x] 桌宠在打字的同时能够同步播放豆包（Doubao）的语音音频，并等待语音播放结束后再进行下一段对话。
- [x] `docs/gameplay_analysis_pyrrha.md` 文档已创建并详细分析了 `Pyrrha.gd` 中的进阶玩法和状态机设计。