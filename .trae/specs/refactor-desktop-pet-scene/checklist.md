- [x] `pet_body.gd` 脚本存在且正确接管 Spine 动画与气泡显示逻辑。
- [x] `desktop_pet.gd` 成功移除了冗余代码并能正确调用 `PetBody`。
- [x] 鼠标穿透逻辑 `_update_mouse_passthrough` 在新结构下运行正常，能够正确囊括背景层、UI层和独立的宠物实体层（以及其中的气泡）。
- [x] 运行游戏并呼出桌宠时，没有节点找不到的报错，且交互功能（聊天、拖拽、点击触发）均正常。
