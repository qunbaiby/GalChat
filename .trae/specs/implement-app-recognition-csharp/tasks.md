# Tasks
- [x] Task 1: Restore C# Project Configuration
  - [x] SubTask 1.1: Update `project.godot` to re-enable C# features and dotnet assembly name (`[dotnet] project/assembly_name="GalChat"`).
  - [x] SubTask 1.2: Generate or create `GalChat.csproj` and `GalChat.sln` for Godot .NET.
- [x] Task 2: Implement `WindowDetector.cs`
  - [x] SubTask 2.1: Create `scripts/csharp/WindowDetector.cs` with P/Invoke signatures (`EnumWindows`, `IsWindowVisible`, `GetWindowText`, `GetWindowThreadProcessId`).
  - [x] SubTask 2.2: Implement logic to enumerate windows and find the top-most valid external application window (filtering out Godot's PID and system windows like `Program Manager`).
- [x] Task 3: Integrate with Desktop Pet
  - [x] SubTask 3.1: In `desktop_pet.gd`, instantiate `WindowDetector.cs` using `load("...").new()` or `add_child()`.
  - [x] SubTask 3.2: Add a polling timer (`_poll_timer`) to periodically fetch the active window title and process ID.
- [x] Task 4: Re-implement Proactive Chat Logic
  - [x] SubTask 4.1: Track time spent on the current external window (`_time_since_last_switch`).
  - [x] SubTask 4.2: Map window titles/processes to application types (e.g., Chrome -> Web Browser).
  - [x] SubTask 4.3: Trigger a DeepSeek chat request (`_trigger_proactive_chat()`) with a prompt tailored to the recognized app and the character's persona.
