- [x] `activity_panel.tscn` 包含 7 个行程槽位，并可以通过点击左侧或下方课程列表将课程填入空槽位。
- [x] 当某个行程槽位被占用时，点击该槽位可以撤销（移除）对应的课程。
- [x] 只有当 7 个槽位全部填满时，才能点击“执行安排”按钮。
- [x] 成功调用 `schedule_execution_panel.tscn`，并进入按天（1 到 7 天）展示的执行界面。
- [x] 在 `schedule_execution_panel.tscn` 中，每次点击屏幕或“继续”按钮，都会向前推演一天，正确执行 `execute_activity` 并展示当天课程和收益（如体能 +15）。
- [x] 当 7 天安排执行完毕后，面板可以被关闭，并且清空了先前的日程数组。