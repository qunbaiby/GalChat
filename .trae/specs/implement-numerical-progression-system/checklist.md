- [x] `character_profile.gd` 包含六大基础属性（默认 0）、每日精力和行动力上限。
- [x] 跨天登录时，角色的精力值自动回满 100（在 `init_daily_mood` 处触发）。
- [x] `stats_system.gd` 能根据基础属性值和给定的加权公式计算并返回“体、智、魅”三大核心维度（向下取整，且基础属性最高 2000）。
- [x] `activity_manager.gd` 包含 6 门必修课程定义，涵盖所有基础属性的收益区间（随机）。
- [x] 玩家能在 `activity_panel` 中点击课程，系统扣除精力并计算收益（有失败/成功提示）。
- [x] `stats_panel` 能实时、直观地反映执行活动后各属性和三大核心维度的变化。
- [x] 所有新数据支持持久化存档，重新启动游戏数据不会丢失。
- [x] 在主界面能通过按钮打开和关闭这两个新面板。