- [x] 创建了 `window_monitor.gd` 用于在 Windows 下读取当前前台窗口标题。
- [x] 桌宠脚本内增加了一个低开销的定时轮询机制，记录当前专注/闲置应用的时长。
- [x] 全局反应冷却系统已实现，桌宠不会在短时间内（如 30 秒内）连续发起多次主动对话。
- [x] 当时间跨越到整点（0-2分）时，桌宠能感知当前时间段并主动生成对应的整点关怀。
- [x] 当用户在某个窗口（非 GalChat 本身）停留时间超过设定阈值（如 3 分钟）时，桌宠会尝试获取窗口标题，并主动发起相关话题聊天。
